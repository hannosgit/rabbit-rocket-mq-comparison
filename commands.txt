docker-compose down && mvn clean install && docker-compose build && docker-compose up -d rabbitmq  && docker-compose  up -d prometheus  && docker-compose  up -d consumer && timeout /T 30 && docker-compose  up -d producer

docker-compose down

docker cp  first-prototype_consumer_1:/project/result.csv ./result.csv